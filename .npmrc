# Expo + pnpm: one flat node_modules like npm/yarn. Metro and Expo autolinking do not handle
# pnpm's default isolated (symlinked) layout. Must be set before the first `pnpm install`.
node-linker=hoisted

# Fail `pnpm install` on a Node version outside package.json engines instead of failing at runtime.
engine-strict=true
